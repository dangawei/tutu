module.exports = {
	/**
	 * routes 生成文件和路由
	 * @type {Array}
	 */
	// routes_old: []

	/**
	 * redirectRoutes 重定向路由
	 * @type {Array}
	 */
	redirectRoutes: [
		{
			from: '/',
			to: '/userSetting'
		}
	]
}
